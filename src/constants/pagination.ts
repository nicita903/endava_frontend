import type { TablePaginationState } from '../components/Table/types';

export const DEFAULT_PAGINATION: TablePaginationState = {
  page: 1,
  size: 10,
};

export const DEFAULT_PAGE_SIZE_OPTIONS = [10, 25, 50, 75, 100];
